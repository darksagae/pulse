# PublicPulse v2.0 - Render Deployment Requirements
# Using only pre-compiled wheels to avoid Rust compilation
# Python 3.11 compatible versions only

fastapi
uvicorn
python-multipart
pydantic
python-dotenv
aiohttp